# Core loop (works on any OS in simulate mode -- no hardware deps needed)
# stdlib only: sqlite3, json, logging, time

# --- Raspberry Pi radio hardware ---------------------------------------
# Installed by deploy setup on the Pi; NOT required for simulate-mode dev
# (imports are guarded with try/except in radio.py).
#
# adafruit-blinka           # board/busio/digitalio -- Pi GPIO/SPI via CircuitPython API
# adafruit-circuitpython-rfm9x   # SX1276/RFM95 driver
